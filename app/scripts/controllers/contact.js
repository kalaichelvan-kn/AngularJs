'use strict';
angular.module('ausampleApp')
  .controller('ContactCtrl',[ "$rootScope", "$scope", function ($rootScope, $scope) {
    // if ($rootScope.loggedIn=="out"){
    //   $state.go('home');
    // }
}]);